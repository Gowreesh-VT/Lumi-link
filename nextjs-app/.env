PORT=3000
BAUD_RATE=115200

# Currently detected ESP32 — plug in both boards and re-run `node scripts/list-ports.js`
# to find the second port, then assign TX_PORT and RX_PORT correctly.
TX_PORT=/dev/tty.usbserial-0001
RX_PORT=/dev/tty.usbserial-0001
